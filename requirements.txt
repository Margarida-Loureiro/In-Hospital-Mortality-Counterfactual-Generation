# Core data / modeling stack (used by preprocessing.py, train_xgboost.py, and
# make_counterfactual_query_batches.py)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0

# Parquet support (write_table() in preprocessing.py / train_xgboost.py falls
# back to CSV automatically if this isn't installed).
pyarrow>=14.0

# Counterfactual generation (only needed for the corresponding pipeline stage)
dice-ml>=0.11          # dice_mean.py, dice_median.py, dice_knn.py
raiutils>=0.4.1        # imported directly by the dice_*.py scripts
pulp>=2.7               # ocean_style.py (bundles the CBC MILP solver)

# facet_style.py uses only the packages above (numpy/pandas/xgboost); it has
# no extra dependency of its own.
